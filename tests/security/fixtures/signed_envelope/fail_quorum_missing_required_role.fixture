# Track-2 reject-matrix row. Executed (not just declared): the named case
# in scripts/test/eval_envelope.py builds this envelope as real bytes and
# runs the in-kernel reader source (envelope_reader.nxh) against it.
checker = signed_envelope
expect = fail
case = COSIGNER_ROLES required_mask missing class-required role (POLICY not in required_mask for ART_APP)
matrix_case = quorum_missing_required_role
